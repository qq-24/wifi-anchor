# WiFi 锚点：室内点位记录与进入判定 —— 技术方案

项目代号 `wifi-anchor`。目标设备：用户的安卓手机（宿舍，每间房一台 AP）。
交付方式：本机 Gradle 出 debug APK → GitHub Release → 手机直接下载安装 → App 内导出文本/文件回传分析。
**手机无法 adb 连接电脑**，所以一切诊断必须在 App 界面内自证、并能以文本形式导出。

置信度标注约定：【已核】= 已查官方文档或权威来源；【已测】= 本机或真机实测得到；【推断】= 按模型/量级推导，尚未在宿舍实测；【假设】= 待探针验证，未测。

---

## 1. 需求与精度档位

用户的真实需求被拆成三件事，它们难度完全不同：

| 编号 | 需求 | 本质 | 难度 |
|---|---|---|---|
| R1 | 记录若干点位（自己宿舍、同层厕所、走廊等） | 存一份该位置的特征数据 | 低 |
| R2 | 之后判断"我在不在某个已记录点位范围内"，进入即判定 | **分类问题** | 中，可行度高 |
| R3 | 给出"离记录点多远"的米数 | **回归问题** | 高，取决于 AP 是否支持 802.11mc |
| R4 | 前台手动测 + 后台常驻（界面可开关），要省电高效 | 受系统扫描配额支配 | 中，有解但必须绕 |
| R5 | 无 adb 条件下完成开发与调试闭环 | 自诊断 + 文本导出 | 低，纯工程 |

**关键判断：R2 是这个场景的甜点位，不是难点。**【推断，待探针证实】

因为每间房一台 AP，"房间"与"同层厕所"这两个相隔约 5 米的位置，其**可见 AP 集合**本身就不同：在房间内，本房间 AP 在 -30 ~ -50 dBm 量级、隔壁房间 -60 ~ -75、走廊 AP 更弱；走到厕所，走廊与本层邻近 AP 反转为最强，自己房间 AP 显著衰减。楼层之间靠楼板衰减 15 ~ 25 dB，且各层 AP 的 BSSID 完全不同——**集合特征直接分层，不需要靠信号强度差**。

也就是说：这个需求不需要那份"技术分析"里承诺的亚米精度。5 米级的"房间 vs 同层厕所"区分，是 RSSI 指纹的舒适工作区。

R3 是真难点，见 §4。

---

## 2. 硬约束（决定了架构，不是可调参数）

1. **扫描配额**【已核，Android 官方 Wi-Fi 扫描文档】
   - Android 9+：前台 App 最多 **4 次扫描 / 2 分钟窗口**；所有后台 App 合计 **30 分钟 1 次**。
   - 超限行为：`startScan()` 返回 `false`，而 `getScanResults()` 返回的是**上一次成功的旧结果**——静默给脏数据，不报错。
   - Android 10+ 有开发者选项"Wi-Fi 扫描限流"可关，但**不能要求用户长期关闭**，只能用于本地调试。
   - 结论：任何"实时检测"设计都必须假定**自己的 startScan 不可靠**，主体数据要从系统被动扫描里蹭。

2. **权限**【已核】
   - Android 10+ 且 targetSdk ≥ 29 时 `ACCESS_COARSE_LOCATION` 不够，`getScanResults()` 需要 `ACCESS_FINE_LOCATION`；缺权限抛 `SecurityException`。
   - **系统定位开关必须打开**（Android 9+），否则扫描恒空。App 必须有一键跳设置的引导页。
   - Android 13+（target 33+）另需 `NEARBY_WIFI_DEVICES`。本项目两个都要：RTT 通道可只靠 NEARBY，指纹通道必须有 FINE。

3. **RTT 的三方条件**【已核，Android 官方 Wi-Fi RTT 文档】
   - 手机侧：Android 9 / API 28 以上，且 `PackageManager.FEATURE_WIFI_RTT` 为真。
   - AP 侧：必须是 **802.11mc（FTM responder）** 或 802.11az（NTB，需 Android 15+）响应器。
   - **这一条不必靠猜：`ScanResult.is80211mcResponder()` 可以在 App 里逐台读出来。** 所以"能不能走 RTT"是一个装完 App 30 秒就能判定的事实。
   - 不需要连接目标 AP；需要系统定位服务开启。官方给出典型精度 **1 ~ 2 米**。

4. **RSSI → 米数的指数放大效应**【推断，量级推导】
   对数路径损耗模型下距离是 dB 的指数函数：`d = d₀ · 10^((RSSI−A)/(−10n))`。取常见量级 n=2.7、阴影衰落 σ≈6 dB，1σ 距离误差为 **×1.67 倍**——真值 3 米对应约 1.8 ~ 5.0 米区间。
   → 因此纯 RSSI 反推的"米"只能当**数量级参考**显示，界面上必须标注为估算，不得呈现为测量值。

5. **上一轮那份分析里的"误差最大可控制在 0.36 米"判定为不可用数字**【未验：查无出处】
   RSSI 指纹的公开结果普遍是米级到房间级；亚米级结论基本建立在 **CSI**（信道状态信息）之上，而普通 App 读不到 CSI（需 Nexmon/专用固件）。故本方案不以亚米为目标，0.36 m 不进入任何设计输入。

---

## 3. 三条信号通道（并行使用，互为备份）

单靠扫描指纹做不到"进入范围立刻判定"（受配额限制），所以用三条刷新率不同的通道组合：

| 通道 | 刷新率 | 信息量 | 配额代价 | 作用 |
|---|---|---|---|---|
| **A. 扫描指纹** `getScanResults()` | 低频（配额 4/2min，或蹭系统被动扫描） | 高（全部可见 AP 向量） | 高 | 定性的"在哪个点位"分类，R2 主判据 |
| **B. RTT 测距** `RttManager` | 中频（一次 ranging 数百 ms） | 真米数 | 不占扫描配额 | R3 的唯一可信来源 |
| **C. 已连接 AP 的 RSSI** `WifiInfo.rssi` | 高频（秒级，不触发扫描） | 低（单台 AP） | 无 | 后台省电的"心跳"，用于检测"我好像动了"再触发一次 A |

C 是这套设计里省电的核心：平时只读 `WifiInfo.rssi`（廉价、可秒级），一旦它偏离基线超过门限，才申请一次完整扫描做确认。这样"进厕所"这类事件能在秒级被发现，而平均功耗接近常亮一个传感器。

**通道 B 的一个重要简化**：用户要的是"离**记录点**多远"，不是绝对坐标。若记录时存下"到 AP₁、AP₂ 的距离"，之后只需再测同样两台 AP 的距离，取差值即可得到相对记录点的位移——**不需要知道任何 AP 的物理坐标**，也不需要 3 台以上 AP 做多点定位。这正是"每间房一台 AP"带来的便利：记录点旁就有一台固定地标。

---

## 4. 算法设计

### 4.1 记录（R1）
一个点位 = 多次会话的合并统计，而不是单帧快照。单次会话 = 在该点原地不动连续采 N 帧（默认 40 帧，间隔取通道 A 的实际可达值）。

每台 AP 存分布统计而非单值：`中位数 m`、`四分位距 iqr`、`出现率 p`、`频段/信道`。
记录时同时写入通道 B 的测距结果（若可用）与环境元数据（时间、电量、是否连着 Wi-Fi、连着哪台 AP）。

**强制要求：每个点位至少 3 个不同时段（早/午/晚）的会话才允许产生阈值。**【设计纪律】
理由：Wi-Fi 指纹真正的死因是跨时间不可复现（AP 自动调信道/功率、开门关门、人员密度）。只在一个下午录完的指纹库，第二天大概率虚警。探针阶段必须先测这个漂移量。

### 4.2 判定（R2）
只用**共见 AP**（记录点与当前帧都出现过的 AP）计算，避免"缺失即差异"造成的假距。

```
集合分量  s_set = |BSSID_cur ∩ BSSID_i| / |BSSID_cur ∪ BSSID_i|        (Jaccard)
强度分量  s_sig = Σ w_k · exp(-|rssi_cur,k − m_i,k| / b) / Σ w_k
          w_k = p_i,k · (1 / (1 + iqr_i,k/σ₀))      // 出现率高、自身抖得小的 AP 权重大
总分      S_i = α·s_set + (1−α)·s_sig
```

判定"在点位 i 内"需同时满足：`S_i ≥ T_in` **且** `S_i − max_{j≠i} S_j ≥ margin`（第二项防止房间/厕所交界处摇摆）。

`α, T_in, margin, b, σ₀` **全部从实测数据拟合，不允许手拍**。探针导出的原始数据回来后，我用"同点位跨时间分布"与"不同点位分布"的分位数定出它们，并把 ROC/混淆矩阵一并给用户看。

输出**分级而非布尔**：命中某点 / 处于两点交界的过渡带 / 不匹配任何已知点（提示"这里可以录成新点位"）。最后一档很重要——宿舍楼里走廊、水房、楼梯口都会自己冒出来。

### 4.3 距离（R3）
- **有 RTT**：`d̂ = |d_cur − d_record|` 逐 AP 给出，多台则做加权最小二乘合并，并附实测不确定度（用该 AP 多轮测距的极差）。这是可显示的**真米数**。
- **无 RTT**：不输出米数，只输出 4.2 的分级 + "参考米数量级（×1.67 倍误差带）"，界面明确写"估算"。

### 4.4 后台常驻（R4）
- 前台服务（`foregroundServiceType` 用 `location`，Android 14+ 强制声明）+ 通知栏可见，界面开关控制。
- 省电策略：常态只跑通道 C；C 越界或到达定时窗口才跑 A；A 的调用受令牌桶控制，配额耗尽时**明确显示"数据来自 X 秒前"而不是假装新鲜**（这是对静默旧数据的唯一诚实做法）。
- 优先蹭系统被动扫描：注册 `SCAN_RESULTS_AVAILABLE_ACTION` 并订阅 `NETWORK_PROVIDER` 位置更新，让系统的扫描喂我们数据。这条路**能不能跑通、刷新率多少，由探针页 2 实测回答**。
- 不做的事：不用 alarm 唤醒疯狂扫描（既费电又必然撞配额）；不假装能亚秒级响应。

---

## 5. Phase 0：探针 App —— 它必须回答的问题

### 5.1 首轮实测结果（2026-09-23 13:17，Xiaomi 23127PN0CC / Android 16 / HyperOS OS3.0.302.0，宿舍内，连着 BISTU-802.1X）

| # | 问题 | 实测值【已测】 | 结论 |
|---|---|---|---|
| Q1 | 手机 RTT | `android.hardware.wifi.rtt = 无`，`WifiRttManager 取不到` | 手机侧无 RTT |
| Q2 | AP 侧 FTM | 71～76 台可见 AP 中 **0 台** `is80211mcResponder` | AP 侧也不支持 → **换手机无用，RTT 通道在本楼关闭** |
| Q3 | RTT 精度 | 无从测 | R3 真米数不可得 |
| Q4 | 扫描配额 | `startScan` 连发 6 次：#01–#04 TRUE、#05–#06 false | 与官方口径一致，**4 次/2 分钟**（Xiaomi 未改）→ 长期最快约 30 s 一次主动刷新 |
| Q5 | 缓存自己会不会刷新 | 24 s 被动观察：读缓存 12 次，时间戳仅前进 **1 次**；广播 1 次且 `resultsUpdated=true`；缓存年龄中位 28.9 s、最长 301 s | 系统确实会替我们扫，但节奏约 20~25 s，且不可依赖；**界面必须显示数据新鲜度** |
| Q6 | 连接 RSSI 心跳 | 10 个 1 s 间隔样本全部有效，均值 −54.4 dBm，**标准差 0.5**，范围 [−55, −54] | 通道 C 成立，是后台省电设计的地基；但抖动这么小也可能因 ROM 更新慢，需在走动时复测 |
| Q7 | 跨时间可复现 | 待采样数据 | 未决 |

**新发现的算法约束（原方案没预料到）**：同一位置站着不动，可见 AP 数在 **50～76 台之间跳**（均值 60.8，标准差 13.4）。
成因【推断】是扫描的信道轮转 —— 单次扫描不覆盖全部信道，所以"集合变小"不等于"位置变了"。
→ §4.2 的集合分量必须按**每台 AP 的跨帧出现率**加权，且判定要用**多次扫描的并集**而不是单帧；
→ 探针的 v0.1 会话录制有缺陷：只读缓存不申请扫描，40 帧里约 5/6 是同一份缓存的重复读。v0.2 已改为贴着 4 次/2 分钟的节奏主动挤扫描，并用 `ScanResult.timestamp` 标记真刷新。

**探针自身的两处不可得**（不影响结论，已在界面上标注原因）：`WifiManager.getSupportedFeatures` 与
`PowerManager.getCurrentStandbyBucket` 均为非公开 API，Android 16 上反射失败；后者对后台设计有用，
正式版改用「实测后台存活」代替读取该状态。`isScanAlwaysAvailable = 是` 已经回答了它想回答的问题。


探针不是 demo，是**测量仪器**。它的唯一任务是把下面 7 个问题的答案用实测数据钉死，然后 §3/§4 的通道选择和阈值才能定。

| # | 问题 | 判据 | 影响 |
|---|---|---|---|
| Q1 | 手机是否支持 RTT | `hasSystemFeature(FEATURE_WIFI_RTT)` | 决定 B 通道存在与否 |
| Q2 | 宿舍 AP 里几台是 FTM 响应器 | 扫描结果中 `is80211mcResponder()` 计数 | **两条路分叉的决定性证据** |
| Q3 | 若能测距，误差多大 | 单 AP 多轮 `distanceMm` 的中位数/极差/成功率 | 极差 < 1 m → 走 RTT 主通道 |
| Q4 | 本机真实扫描配额 | 连续 `startScan()` 返回值时间线；有前台服务时是否放宽 | 决定 A 通道可用刷新率 |
| Q5 | 缓存新鲜度 | 不主动扫描时 `ScanResult.timestamp` 是否自行变化；`SCAN_RESULTS_AVAILABLE` 3 分钟内到达次数 | 决定后台能否蹭到数据 |
| Q6 | `WifiInfo.rssi` 采样率与噪声 | 秒级连续读 60 s，统计方差与断流 | 决定 C 通道心跳可行性 |
| Q7 | 跨时间可复现性 | 同一位置隔 1 h / 1 天各录一组会话，回传后离线比对 | **指纹方案的生死判据** |

### 结论规则（预先写下，不许事后找补）

**当前状态（2026-09-23）：第 1 条被实测否掉，第 2 条已生效** —— 手机与 AP 两侧都无 FTM，
所以正式版按「指纹通道为主 + 连接 RSSI 心跳」构建，R3 只给分级与标注为估算的参考米数。
剩余待测：Q7（跨时间可复现性），它决定 R2 的阈值与是否需要每日重录。
- Q2 命中 ≥1 台且 Q3 极差 < 1 m → **RTT 为主通道**，R3 给真米数，R2 用 RTT + 指纹融合，精度预期 1–2 m。
- Q2 为 0（极可能是这个结果：宿舍 AP 多为运营商/校园定制机，FTM 默认关闭）→ **退指纹通道**，R2 以房间/厕所级分类为目标，R3 降级为分级 + 标注估算的参考米数。
- 若 Q7 显示同点位跨天漂移大到与"房间 vs 厕所"的差异同量级 → **诚实判定 R2 需每日重录**，并给出增量校准方案（用最近一次已知判定反推每台 AP 的偏移量），而不是嘴上说"能定位"。
- 三条都不成立才会谈替代方案（BLE beacon / UWB），且必须附"已试 X 得到 Y、满足 Z 可翻案"的证据，不许一句"做不了"收尾。

---

## 6. 探针的数据导出格式（无 adb 的闭环）

原始记录一律 JSONL，一行一帧，**不做预汇总**——判断交给离线分析，避免在 App 里替我下结论。
三条导出通道：SAF 保存到 Downloads（全量文件）、系统分享、复制到剪贴板（截断版，可直接粘给我）。

```json
{"t":1729700000123,"ev":"frame","sess":"room-desk","seq":12,
 "scan_age_ms":4200,"from":"passive|own_scan|cached",
 "ap":[["aa:bb:cc:dd:ee:01","DORM-2F-01",-47,5220,"[ESS][FTM]",true],
       ["aa:bb:cc:dd:ee:02","DORM-2F-02",-71,2437,"[ESS]",false]],
 "conn":{"ssid":"...","bssid":"aa:bb:cc:dd:ee:01","rssi":-49,"freq":5220,"id_ok":true},
 "bat":87,"screen_on":true,"loc_on":true,"perm_fine":true}
{"ev":"rtt","sess":"room-desk","bssid":"aa:bb:cc:dd:ee:01","dist_mm":1834,"rssi":-49,"mac":"rand?","ok":true,"round":3}
{"ev":"quota","i":4,"ret":false,"elapsed_ms":1180,"window":"fg"}
{"ev":"meta","device":"...","sdk":34,"feat_rtt":false,"scan_always":true,"note":"..."}
```

回传约定：探针跑完 → 导出 `probe-<日期>-<点位>.jsonl` → 发给我 → 我出实测报告与阈值 → 再进 Phase 1。

---

## 7. 工程与环境（本机已核实）

- Android SDK：`C:\Users\mingh\AppData\Local\Android\Sdk`（platforms 至 android-37，build-tools 至 36.0.0，含 NDK 与 platform-tools/adb）。
- JDK：`D:\jdk23`（java 23.0.2）。Gradle：wrapper 8.9 已在 `~\.gradle\wrapper\dists` 缓存中。
- **版本组合直接复用 `Moniter_apk_plus`**（AGP 8.3.0-rc02 / Kotlin 1.9.0 / Compose BOM 2023.08.00 / compileSdk 34 / minSdk 24），该工程有成功产出的 `app-debug.apk`，等于构建链已被验证；**不引入任何新依赖**，JSON 用 Android 内置 `org.json`，存储用 `SharedPreferences` + 应用私有文件，避免联网拉包失败。
- 项目路径必须是**纯 ASCII**（`D:\mingh\Documents\wifi-anchor`）。已知的坑：中文 + `==` 路径会让 Gradle 阶段失败，必须 `subst V:` 才能构建——本项目从一开始就避开这个雷。
- `gradle.properties` 沿用 `-Xmx2048m -Dfile.encoding=UTF-8`、`android.useAndroidX=true`、`android.nonTransitiveRClass=true`。
- 发布：`gh` 已登录（账号 `qq-24`，token 具备 `repo` 权限）。新建公开仓库，源码 + SPEC 一起推，APK 挂 Release。

## 8. 里程碑

1. **M0 探针**（当前）：5 个页面——能力诊断 / 扫描配额与新鲜度 / RTT 测距 / 命名点位连拍 / 导出与日志。装到手机上跑完 Q1–Q7。
2. **M1 分析**：拿回的数据出实测报告——同点位漂移、点位间可分度、配额真相、RTT 精度；定出 `α, T_in, margin` 与通道取舍。
3. **M2 正式版**：点位管理（记录/合并多会话/删除）、实时匹配界面（命中/过渡/未知 + 距离 + 数据新鲜度）、后台开关与前台服务、阈值自检与重录提醒、导出历史。
4. **M3 验证**：真实生活数据跑 3 天——每天在宿舍/厕所/走廊/别的楼层来回，统计虚警与漏检，再决定是否引入增量校准。

## 9. 风险登记（含翻案条件）

| 风险 | 当前判断 | 翻案条件 |
|---|---|---|
| 宿舍 AP 不支持 FTM | 【假设】概率偏高，未测 | 探针 Q2 若有一台响应器即翻案，R3 立刻可做真米数 |
| 扫描配额把 A 通道压死 | 【已核】规则存在，实际影响未测 | Q5 若显示系统被动扫描能高频刷新，则影响大幅缩小 |
| 跨天 RSSI 漂移淹没 5 米差异 | 【假设】最大风险 | Q7 数据回来算比值；漂移 > 点位间差异则改为"每日快速重录 + 增量校准" |
| 手机与 AP 之间随机 MAC/私有地址 | 对扫描结果无影响（AP 的 BSSID 是真地址） | 若某 ROM 篡改 BSSID 显示，改用 SSID+信道做键 |
| OEM ROM 的 Compose/权限怪癖 | 未测 | 探针装机后按日志逐项排 |
